# records-sandbox (eval fixture)

Frozen, pre-onboarded sandbox project for the lcd prompt-eval benchmark: a tiny
records app with a CLI and an HTTP server sharing one core module, plus a green
`node --test` suite. The runner copies this directory once per run — do not edit it in place.
