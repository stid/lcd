# Cross-Path Audit: <FEATURE_TITLE>

> **Deep lane · Phase 6 (Cross-Path Audit).** Generated by `/lcd:audit <slug>`. Read-only artifact — do not edit by hand.

**Slug:** `<SLUG>`
**Spec:** [`spec.md`](./spec.md) • **Plan:** [`plan.md`](./plan.md) • **Tasks:** [`tasks.md`](./tasks.md)
**Run:** <YYYY-MM-DD HH:MM>
**Result:** PASS <!-- PASS | BLOCKED -->

---

## Surface coverage

| AC | Surface | Handler hit | Test hit | Status |
|----|---------|-------------|----------|--------|
| `AC-1` | CLI | `<file>:<line>` | `<test-file>:<line>` | OK |
| `<…>` | | | | |

## Status legend

- **OK** — the plan-declared path resolves (file exists, and contains the declared token if one was given) AND a test carrying the literal `AC-N (SURFACE)` exists.
- **MISSING-HANDLER** — the plan-declared path does not resolve. Wire the handler or fix the plan's path cell.
- **MISSING-TEST** — handler resolves but no test carries `AC-N (SURFACE)`. Generate or rename the test.
- **MISSING** — both the handler and the test are absent.
- **BLOCKED** — `plan.md` has no matrix row for this (AC, surface) pair. Update the plan first.

## Gate

If any row's status is not `OK`, the audit exits non-zero and PR creation is blocked. Fix the offending rows, re-run `/lcd:audit <slug>`, and confirm `Result: PASS` before proceeding.
