# Red-Green Loop Stuck: <FEATURE_TITLE>

> **Phase 5 escalation.** Generated by `lcd:redgreen-loop` when the same test failed twice in a row OR the same fix diff was attempted twice. The user must intervene; the loop will not retry automatically. This file plus the work-item `JOURNAL.md` are enough for a cold pickup.

**Slug:** `<SLUG>`
**Last attempted:** <YYYY-MM-DD HH:MM>
**Iteration count when halted:** <N> / 20
**Halt reason:** <repeat-failure | repeat-diff | iteration-cap>

---

## Failing test

```
<test name / id as the runner printed it, e.g. AC-3 (HTTP): /api/version returns build info>
```

**File:** `<test-file>:<line>`

## Last assertion error

```
<copy the exact failure block the test runner printed>
```

## Last three attempted diffs

### Attempt N (latest)

```diff
<unified diff>
```

### Attempt N-1

```diff
<unified diff>
```

### Attempt N-2

```diff
<unified diff>
```

## Suggested next steps

1. Read the failing test and confirm its assertion matches the AC body in `spec.md`.
2. Look at the three attempted diffs — they likely converge on a wrong mental model. Identify what's being missed.
3. Either fix the implementation manually (the loop resumes on next invocation), or rewrite the test to match a corrected understanding of the AC (then update `spec.md` to match).
4. Re-run the red-green loop or finish manually.

## Edit boundary (from JOURNAL EDIT BOUNDARY / plan.md File-structure)

The loop was restricted to these paths. If the fix requires editing outside this set, update `plan.md`'s File structure and the JOURNAL EDIT BOUNDARY first, then re-run.

- `<path>`
- `<path>`
