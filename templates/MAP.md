# MAP — <PROJECT_NAME>

> **Logical-organization guardrails.** Read this FIRST on any cold start, before planning new
> work. It exists so the AI (and you) don't re-derive the project's shape every session — and
> don't reinvent a place that already exists. Generated by `/lcd:onboard` from the *actual*
> structure; refined by `lcd:refine` when reality drifts. Keep under ~150 lines.

**Project type:** <software | content-store | docs | other>
**Generated:** <YYYY-MM-DD> · **Last refined:** <YYYY-MM-DD>

---

## Shape

<2–4 sentences: what this project is and its top-level organizing scheme. For an app: the
architecture style (layered, hexagonal, feature-sliced, monorepo packages…). For a non-code
store (e.g. a recipe collection): the filing scheme and how items are split / named / indexed.>

## Zones / Components

> One row per top-level area. "Don't put here" is the guardrail that prevents sprawl.

| Zone | Path | Responsibility | Don't put here |
|---|---|---|---|
| <e.g. api> | `<src/api/>` | <HTTP handlers, request/response shaping> | <business logic, persistence> |
| <e.g. core> | `<src/core/>` | <domain logic, pure functions> | <I/O, framework deps> |
| <e.g. tests> | `<tests/ or co-located>` | <how/where tests live> | — |

## Entry points

- <run / build / serve command and the file it boots from>
- <CLI entry, server entry, worker entry — whichever apply>

## Surfaces in use

<Subset of `CLI | HTTP | MCP | RENDER | DB | EVAL` that this project actually exposes, or
"n/a — non-software". This is the vocabulary the Deep lane's cross-path matrix draws from.>

## Invariants (always-true rules for THIS project)

> Recurring rules that hold across all work here (LCD principle: don't relitigate them each
> time). The Deep lane's Constitution check and the red-green loop honor these.

- <e.g. PR opened → address review comments in iterations until all resolved before merge>
- <e.g. every new HTTP endpoint gets an OpenAPI entry + an HTTP-surface AC>
- <e.g. design tokens only — no hardcoded colors/spacing>
- <e.g. version manifest bumped in the first commit of a branch>

## Conventions pointer

Toolchain, test placement/discovery, and gate commands live in
`.claude/rules/lcd-conventions.md` (machine block). Decisions log: see `DECISIONS.md`. This MAP is
*structural* (where things live); if `living-spec: on`, the *behavioural* current-state index
("what the system does now") is `SPEC.md`.

<!--
Refresh triggers (when to re-run /lcd:onboard or let lcd:refine touch this file):
  - A new top-level zone/module appears that isn't in the table.
  - A "Don't put here" guardrail is now routinely violated (the rule moved).
  - A new surface starts being exposed.
  - An invariant is added or retired.
Keep it a map, not documentation — if a row needs a paragraph, it belongs elsewhere.
-->
